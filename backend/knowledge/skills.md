# Skills

Ankit's verified public work supports a full-stack product engineering profile with a strong recent focus on AI-backed portfolio infrastructure, backend APIs, frontend interfaces, data-backed applications, and developer tooling.

Frontend skills represented in the current portfolio include React, Vite, TypeScript, Tailwind CSS, shadcn-style components, TanStack Query, responsive UI layout, chat interfaces, loading and error states, and small interaction details. Public repositories also show TypeScript and JavaScript UI work through `streamify`, `constraint-layouter`, `pulley-first-screen`, `random-password-generator`, `investorbook`, and `ankojh.github.io`.

Backend skills represented in `portfolio-v2` include FastAPI, Python, async SQLAlchemy, asyncpg, Postgres, Alembic migrations, API health checks, CORS configuration, request validation, protected admin endpoints, request/event logging, and app-level rate limiting.

AI and retrieval skills represented in `portfolio-v2` include retrieval augmented generation over Markdown knowledge files, OpenAI embeddings, pgvector similarity search, grounding answer generation in retrieved source chunks, and rejecting unrelated questions before expensive model calls. The assistant should describe this as practical RAG implementation rather than claiming broad AI expertise beyond the project evidence.

Data, visualization, and experimentation skills are represented by public repositories such as `f1-immersion`, a Python project for 3D plotting cars from historical F1 sessions; `DriftMap`, a Swift project for mapping cursor drift; and `TravelGuide`, a semantic-web/big-data travel-guide project.

Older public work shows exposure to Java/Android coursework through `ACS-Android`, machine-learning image classification through `Dolphin-or-Seahorse`, and on-device utility design through `random-password-generator`.

The strongest current stack to mention is TypeScript, React, Python, FastAPI, Postgres, pgvector, OpenAI APIs, Tailwind CSS, Vite, TanStack Query, SQLAlchemy, asyncpg, Alembic, Docker Compose, Vercel, and Render. Swift, JavaScript, Java, Lua, HTML, Web Ontology Language, and Python also appear in public repository metadata.
