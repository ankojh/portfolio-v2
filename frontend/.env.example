# Leave this blank locally to use the Vite /api proxy.
# Set this on Vercel to your Render backend URL, for example:
# VITE_API_BASE_URL=https://api.ankojh.com
VITE_API_BASE_URL=

# Local/Compose-only proxy target for Vite dev server.
VITE_DEV_PROXY_TARGET=http://localhost:8000
